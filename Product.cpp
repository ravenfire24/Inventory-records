#include "Product.h"

// Constructor implementation for the Product class
Product::Product(int _id, string _name, int _quantity, double _price)
    : id(_id), name(_name), quantity(_quantity), price(_price) {}
